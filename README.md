# 🚀 Project Setup Guide

Этот проект разворачивается с помощью **Docker** и **Docker Compose**.

## 📦 Требования

Перед началом убедитесь, что у вас установлено:
- **Git**
- **Docker**
- **Docker Compose**

👉 Если Docker не установлен, скачайте и установите его отсюда:  
🔗 [Docker](https://www.docker.com/get-started)

---

## 📥 Клонирование репозитория

Склонируйте проект к себе на компьютер:

```bash
git clone https://github.com/noportim/to-do-list.git
```

Перейдите в папку проекта:

```bash
cd to-do-list
```

---

## ⚙️ Настройка .env
В корне проекта создайте файл .env и добавьте в него следующие переменные:

```
DB_NAME=your_database_name
DB_PASSWORD=your_database_password
```

Проект использует MongoDB.
Если у вас ещё нет базы данных, вы можете создать её здесь:

🔗 [MongoDB](https://www.mongodb.com/atlas/database)

---

## 🐳 Запуск проекта через Docker
В корне проекта выполните команду:

```bash
docker compose up -d
```

После этого:

- Docker скачает необходимые образы
- Контейнеры будут запущены в фоновом режиме

**Сайт станет доступен 🎉**

---

## 🛑 Остановка проекта
Чтобы остановить контейнеры, выполните:

```bash
docker compose down
```
